server:
  port: ${PORT:8092}
  servlet:
    context-path: /bcb


spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: mypassword
  config:
    additional-location: ${SPRING_CONFIG_ADDITIONAL_LOCATION}