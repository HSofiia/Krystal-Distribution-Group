package be.kdg.prog4.domain;

public enum ChargeType {
    STORAGE,   // “Storage price per ton per day”
    COMMISSION // “1% commission when ship leaves (on PO total)”
    // add later if needed: WEIGHING, HANDLING, APPOINTMENT_FEE, ...
}
